<Window x:Class="空载负载.WaitWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:空载负载"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Title="WaitWindow" Height="150" Width="500">
    <Grid>
        <Border Background="White" CornerRadius="10" Padding="20" Opacity="0.9"   >
            <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Width="400" VerticalAlignment="Center">
                <TextBlock Text="" x:Name="textBox" FontSize="30" FontWeight="Bold" Margin="0,0,0,10"/>
                <ProgressBar IsIndeterminate="True" Height="30" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
