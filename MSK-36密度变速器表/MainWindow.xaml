<Window x:Class="MSK_36密度变速器表.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MSK_36密度变速器表"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Validation.ErrorTemplate">
                <Setter.Value>
                    <ControlTemplate>
                        <StackPanel>
                            <AdornedElementPlaceholder />
                            <ItemsControl ItemsSource="{Binding}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock
                                           Foreground="Red"
                                           Text="{Binding ErrorContent}" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="当前设备号" FontSize="20" Margin="10 0"></TextBlock>
            <!--
            <TextBox Text="{Binding Shebehao2}" FontSize="20"  Width="50" Margin="10 0"></TextBox>
            -->
            <Button Content="得到设备号码" FontSize="20" Command="{Binding CommandGetDevel}"></Button>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="设置当前设备号:" FontSize="20"  ></TextBlock>
            <TextBox x:Name="ShebehaoEdit2" Text="{Binding Shebehao, NotifyOnValidationError=true, ValidatesOnDataErrors=true}" FontSize="20"  Width="50" Margin="10 0"></TextBox>
            <Button Content="确认" FontSize="20"  Width="50" Margin="10 0" Command="{Binding CommandSetDevel}"></Button>

            <Button Content="测试WAIT" FontSize="20"  Width="50" Margin="10 0" Command="{Binding CommandWait}"></Button>

        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="当前设备号:" FontSize="20"  ></TextBlock>
            <TextBlock Text="{Binding CurrentNumber}" FontSize="20" Width="50" Margin="5 0"></TextBlock>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <ComboBox Width="100" FontSize="18" SelectedItem="{Binding SelectItem}" ItemsSource="{Binding ComboBoxItems}"></ComboBox>
            <Button Content="打开串口" FontSize="20" Margin="10 0" Command="{Binding CommandOpen}"></Button>
            <Button Content="关闭串口" FontSize="20"  Margin="10 0" Command="{Binding CommandClose}" ></Button>
        </StackPanel>

        <Grid Grid.Column="1" Grid.Row="0" Grid.RowSpan="4">
            <Border>
                <ListBox ItemsSource="{Binding ListBoxData}" Grid.Column="1" FontSize="15" />
            </Border>
        </Grid>


        
    </Grid>
</Window>
