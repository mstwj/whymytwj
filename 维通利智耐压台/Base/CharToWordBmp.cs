using System;


namespace 维通利智耐压台.Base
{
    class CharToWordBmp
    {
        /*
        public void test(string savepng)
        {
            // 创建一个新的文档实例
            Document doc = new Document();

            // 创建一个构建器来操作文档
            DocumentBuilder builder = new DocumentBuilder(doc);


            // 使用DocumentBuilder插入文字
            builder.InsertParagraph(); // 插入一个新段落
            builder.Font.Bold = true; // 设置字体为粗体
            builder.Font.Size = 14; // 设置字体大小为14磅
            builder.Write("Hello, World!"); // 写入文字


            // 指定图片的路径
            string imagePath = savepng;

            // 插入图片到文档中
            Shape shape = builder.InsertImage(imagePath);

            // 可选：设置图片的尺寸
            shape.Width = 80; // 设置宽度为200磅（大约是72点）
            shape.Height = 80; // 设置高度为200磅（大约是72点）
            shape.WrapType = WrapType.None; // 设置图片环绕方式为无环绕
            
            // 保存文档
            doc.Save("D:\\耐压报表\\output.docx");

        }
        */
    }
}
