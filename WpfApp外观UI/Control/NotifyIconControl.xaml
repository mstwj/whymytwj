<UserControl x:Class="WpfApp外观UI.Control.NotifyIconControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp外观UI.Control"
             xmlns:ws="https://github.com/WPFDevelopersOrg/WPFDevelopers.Minimal"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Button Content="Wait" Width="100" Height="100" Click="Button_Click"></Button>
        <Button Grid.Column="1" Style="{StaticResource PrimaryButton}" Content="WaitAndExit" Width="100" Height="100" Click="Button_Click_1"></Button>
        <Button Grid.Column="2" Style="{StaticResource DropDownButtonStyle}" Content="WaitAndExit" Width="100" Height="100" Click="Button_Click_2"></Button>

        <WrapPanel Margin="0,10" Grid.Column="3">
            <RadioButton Content="Option A"/>
            <RadioButton Content="Option B" Margin="10,0" IsChecked="True"/>
            <RadioButton Content="Option C" IsEnabled="False"/>
        </WrapPanel>

        <WrapPanel Margin="0,10" Grid.Column="4">
            <CheckBox Content="Option A"/>
            <CheckBox Content="Option B" Margin="10,0" IsChecked="True"/>
            <CheckBox Content="Option C" IsChecked="{x:Null}"/>
            <CheckBox Content="Option D" Margin="10,0" IsEnabled="False"/>
        </WrapPanel>


        <WrapPanel Margin="0,10" Grid.Row="1" Grid.Column="1">
            <TextBox/>
            <TextBox Margin="10,0" ws:ElementHelper.Watermark="请输入内容"/>
            <TextBox IsEnabled="False"/>
        </WrapPanel>


        <WrapPanel Margin="0,10" Grid.Row="1" Grid.Column="2">
            <ComboBox Width="200">
                <ComboBoxItem>Option 1</ComboBoxItem>
                <ComboBoxItem>Option 2</ComboBoxItem>
                <ComboBoxItem>Option 3</ComboBoxItem>
                <ComboBoxItem>Option 4</ComboBoxItem>
                <ComboBoxItem>Option 5</ComboBoxItem>
            </ComboBox>
        </WrapPanel>

        <WrapPanel Margin="0,10" Grid.Row="1" Grid.Column="3">
            <ToggleButton/>
            <ToggleButton Margin="10,0" IsEnabled="False"/>
            <ToggleButton IsChecked="True"/>
        </WrapPanel>

        <ws:NotifyIcon Title="WPF开发者" Name="WpfNotifyIcon">
            <ws:NotifyIcon.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="托盘消息" Click="SendMessage_Click"/>
                    <MenuItem Header="闪烁" Name="menuItemTwink"  Click="Twink_Click"/>
                    <MenuItem Header="关于" Click="About_Click">
                        <MenuItem.Icon>
                            <Path Data="{StaticResource PathWarning}" 
                                      Fill="{DynamicResource PrimaryNormalSolidColorBrush}"
                                      Stretch="Uniform" Height="20" Width="20"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="退出" Click="Quit_Click"/>
                </ContextMenu>
            </ws:NotifyIcon.ContextMenu>
        </ws:NotifyIcon>
        <TextBlock Grid.Row="1" Text="123123123"></TextBlock>
    </Grid>
</UserControl>
